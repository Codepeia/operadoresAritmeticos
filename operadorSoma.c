#include<stdio.h>

main(){

    //operadores aritmeticos
    //soma(+), subtração(-), divisao(/)
    //multiplicacao(*), modulo(%), potencia(**)
    //atencao à ordem de precedencia

    int num1 , num2 , soma;

    printf("Digite dois numeros inteiros");
    scanf("%d %d", &num1, &num2);

    soma = num1 + num2;

    printf("A soma e: %d", soma);

  
}